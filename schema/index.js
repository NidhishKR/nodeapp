var candidates = require('./signUpSchema');
var signUpUserJobChoice = require('./signUpUserJobChoiceSchema');
var schemaOutput = {
  candidates : candidates,
  signUpUserJobChoice : signUpUserJobChoice
}
module.exports = schemaOutput;
